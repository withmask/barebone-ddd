export const eventsManagerTokens = {
  repositories: {
    events: 'shared:event-manager:repos:events',
    eventsFailure: 'shared:event-manager:repos:event-failures',
    eventsHandlers: 'shared:event-manager:repos:event-handlers'
  }
};
